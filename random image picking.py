import random

def random_image_picking_R(set_of_images):
  number=random.choice([0,1])
  return(set_of_images[number])

def random_image_picking_A(set_of_images):
  number=random.choice([0,1,2])
  return(set_of_images[number])

